# labels/

No separate label file is used. Labels are folder-based: in `../data/`, each
scenario folder name is the activity label for every frame inside it. See
`../data/README.md` and the scenario-to-class table in `../README.md`.

(If you later export a per-frame CSV, place it here as `labels.csv`.)
